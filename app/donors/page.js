import PagePlaceholder from "../../components/layout/PagePlaceholder";

export const metadata = {
  title: "Donor Search | BloodConnect",
};

export default function DonorsPage() {
  return (
    <PagePlaceholder
      title="Donor Search"
      description="Search registered donors by name, blood group, and district. Filtering logic will be implemented in a later milestone."
    />
  );
}